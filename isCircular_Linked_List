bool isCircular(Node *head)
{
    Node* temp=head;
    
    while(1)
    {
        temp=temp->next;
        if(temp==head)
        {
            return true;
        }
        if(temp==NULL)
        {
            return false;
        }
    }
    
    
}